<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layoutClie"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:openDrawer="start">

    <include
        layout="@layout/app_bar_cliente_menu"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_viewClie"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:fitsSystemWindows="false"
        android:layout_gravity="start"
        app:headerLayout="@layout/nav_header_cliente_menu"
        app:menu="@menu/activity_cliente_menu_drawer"

        app:itemHorizontalPadding="10dp"
        app:itemTextColor="@color/ColorEditText"
        app:itemIconTint="@color/ColorIconEditText"
        app:itemIconPadding="40dp"
        android:background="@drawable/designitems"
        />

</androidx.drawerlayout.widget.DrawerLayout>
